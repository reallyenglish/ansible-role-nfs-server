- hosts: localhost
  roles:
    - ansible-role-nfs-server
  vars:
    nfsd_exports:
      - /usr/local -network 192.168.1.0/24
    nfsd_mountd_flags: -r
    nfsd_flags: -n 4
    nfsd_rpc_flags: "-h {{ ansible_default_ipv4.address }}"
